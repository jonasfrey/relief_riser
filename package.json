{
  "name": "relief-riser",
  "private": true,
  "version": "0.1.0",
  "description": "Convert 2D images into 3D relief plates for FDM printing. Fully client-side.",
  "type": "module",
  "scripts": {
    "start": "[ -d node_modules ] || npm install && vite",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.160.0"
  },
  "devDependencies": {
    "playwright-core": "^1.60.0",
    "vite": "^5.4.0"
  }
}
